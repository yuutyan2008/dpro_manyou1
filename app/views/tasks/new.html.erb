<h1><%= t('view.tasks.new.title') %></h1>

<%# button_label に "登録する" を渡す %>
<%= render 'form', task: @task, button_label: t('helpers.submit.create') %>

<%= link_to t('common.back'), tasks_path, id: 'back' %>
