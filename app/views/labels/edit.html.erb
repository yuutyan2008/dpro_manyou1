<h1><%= t('view.labels.edit.title') %></h1>

<%= render 'form', label: @label, url: label_path(@label), method: :patch, button_label: t('helpers.submit.update') %>

<%= link_to t('common.back'), labels_path, id: "back" %>
